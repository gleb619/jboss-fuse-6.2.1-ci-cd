version: "2"

services:
  nexus:
    image: sonatype/nexus3
    volumes:
      - "nexus-data:/nexus-data"
    ports:
      - "8081:8081"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
  
volumes:
  nexus-data: {}